[project]
name = "money"
version = "0.1.0"
description = "Going to build a portfolio GUI based upon SchwabAPI"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "nicegui>=3.8.0",
    "requests>=2.32.5",
    "yfinance>=1.2.0",
]
